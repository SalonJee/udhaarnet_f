# App Architecture & Flow Diagram

## 🏗️ Application Architecture

```
┌─────────────────────────────────────────────────────────┐
│                      App.js (Main Entry)                 │
│                  (AuthProvider Wrapper)                  │
└──────────────────────┬──────────────────────────────────┘
                       │
           ┌───────────┴───────────┐
           │                       │
    ┌──────▼──────┐         ┌─────▼──────┐
    │ AuthContext │         │AppNavigator│
    │ (State Mgmt)│         │ (Navigation)│
    └──────┬──────┘         └─────┬──────┘
           │                       │
    ┌──────▼────────────────────┐ │
    │ Global Auth State:        │ │
    │ - userRole                │ │
    │ - isAuthenticated         │ │
    │ - user (email)            │ │
    │ - selectRole()            │ │
    │ - login()                 │ │
    │ - logout()                │ │
    └──────────────────────────┘ │
                                  │
        ┌─────────────────────────┼─────────────────────────┐
        │                         │                         │
   ┌────▼─────┐              ┌───▼────┐              ┌─────▼──┐
   │Non-Auth  │              │On Auth │              │On Auth │
   │Screens   │              │Buyer   │              │Seller  │
   └─────┬────┘              └────┬───┘              └──┬─────┘
         │                         │                    │
    ┌────┴──────┬─────────┐   ┌────▼────┐          ┌───▼────┐
    │            │         │   │         │          │        │
┌──▼──┐  ┌──────▼─┐  ┌───▼───┐│         │          │        │
│Role │  │ Login  │  │Buyer  ││  Home  │          │Seller  │
│Sel  │  │ Screen │  │Home   ││        │          │Home    │
│     │  │        │  │       ││        │          │        │
└─────┘  └────────┘  └───────┘│        │          │        │
                               └────────┘          └────────┘
```

## 🔄 User Flow Diagram

```
START
  │
  ▼
┌─────────────────────────────────────┐
│   Role Selection Screen             │
│  ┌─────────────┬───────────────┐   │
│  │   BUYER     │    SELLER     │   │
│  │   (Blue)    │   (Orange)    │   │
│  └──────┬──────┴───────┬───────┘   │
└─────────┼──────────────┼───────────┘
          │              │
    [User clicks role]   │
          ▼              ▼
      ┌─────────────────────────────┐
      │    Login Screen             │
      │ Shows selected role         │
      │ Email input                 │
      │ Password input              │
      │ [LOGIN button]              │
      │ [Back to Role Selection]    │
      └──────────┬────────┬─────────┘
                 │        │
        [Login]  │        │ [Back]
                 │        │
         ┌───────▼┐      └────┐
         │        │           │
    ┌────▼─────┐  │      ┌────▼──────┐
    │ BUYER    │  │      │ Role      │
    │ HOME     │  │      │ Selection │
    │ SCREEN   │  │      │ (restart) │
    └────┬─────┘  │      └───────────┘
         │        │
    ┌────▼────────▼───────┐
    │ Browse Products     │
    │ - Categories        │
    │ - Featured Items    │
    │ - Add to Cart       │
    │ - [Logout button]   │
    └─────────────────────┘
         │
      [Logout]
         │
         ▼
    Role Selection
    (restart flow)

---

    ┌──────────────────┐
    │ SELLER           │
    │ HOME SCREEN      │
    └────┬─────────────┘
         │
    ┌────▼────────────────────────┐
    │ - View Statistics           │
    │ - Manage Orders             │
    │ - Add Products              │
    │ - Manage Inventory          │
    │ - [Logout button]           │
    └─────────────────────────────┘
         │
      [Logout]
         │
         ▼
    Role Selection
    (restart flow)
```

## 📦 Component Hierarchy

```
App.js
│
├── AuthProvider (Context)
│   │
│   └── AppNavigator
│       │
│       ├── Stack Navigator
│       │   │
│       │   ├── [Non-Authenticated Routes]
│       │   │   ├── RoleSelectionScreen
│       │   │   └── LoginScreen
│       │   │
│       │   └── [Authenticated Routes]
│       │       ├── BuyerHomeScreen (if role === 'buyer')
│       │       └── SellerHomeScreen (if role === 'seller')
│       │
│       └── Navigation State
│           ├── isAuthenticated
│           └── userRole
```

## 🗂️ File Organization

```
frontend/
│
├── context/
│   └── AuthContext.js
│       ├── AuthProvider Component
│       ├── useAuth Hook
│       └── Auth State Logic
│
├── screens/
│   ├── RoleSelectionScreen.js
│   │   └── Role selection UI
│   │
│   ├── LoginScreen.js
│   │   ├── Email input
│   │   ├── Password input
│   │   └── Dummy login logic
│   │
│   ├── BuyerHomeScreen.js
│   │   ├── Welcome header
│   │   ├── Categories section
│   │   ├── Products section
│   │   └── Logout button
│   │
│   └── SellerHomeScreen.js
│       ├── Welcome header
│       ├── Stats cards
│       ├── Orders section
│       ├── Action buttons
│       └── Logout button
│
├── components/
│   └── (Future: Reusable components)
│
└── styles/
    └── (Future: Shared style constants)
```

## 🎯 Data Flow

```
┌─────────────────────────────────────────────────────────┐
│              AuthContext (Global State)                 │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  State:                    Methods:                     │
│  ┌─────────────────┐     ┌──────────────────┐         │
│  │ userRole        │     │ selectRole()     │         │
│  │ isAuthenticated │     │ login()          │         │
│  │ user            │     │ logout()         │         │
│  └─────────────────┘     └──────────────────┘         │
│                                                         │
└────────────────┬─────────────────────────────────────┘
                 │
      useAuth() hook (access from any component)
                 │
    ┌────────────┼────────────┬───────────────┐
    │            │            │               │
    ▼            ▼            ▼               ▼
  RoleSel    LoginScreen  BuyerHome      SellerHome
  Screen     Screen       Screen         Screen
```

## 🔐 Authentication Flow

```
START
  │
  ├─ Check isAuthenticated?
  │  └─ No  ──────────────────┐
  │                            │
  └─ Yes ─────────────────────┐
                               │
                    ┌──────────┴──────────┐
                    │                     │
                    ▼                     ▼
            Show Role Selection     Check userRole
              Screen               
                    │                     │
              [User selects]        ┌─────┴─────┐
                    │               │           │
                    ▼              buyer      seller
            Store role in                │
            AuthContext             ┌───┴────────┐
                    │               │            │
                    ▼            buyer home   seller home
            Navigate to            screen     screen
            LoginScreen                │          │
                    │            [User actions] │
              [User inputs]            │          │
              email & password         │          │
                    │            [Logout click]  │
                    ▼                 │          │
            selectRole() &            │          │
            login() calls             │          │
                    │                 │          │
                    ▼                 │          │
            Update auth state         │          │
            Navigate to home          │          │
                    │            ┌────┴────┐    │
                    └────┬───────┤  Reset   │────┘
                         │       │ Auth     │
                         │       │ State    │
                         │       └─────┬────┘
                         │             │
                         └─────┬───────┘
                               │
                               ▼
                        Role Selection
                        (Loop restart)
```

## 🎨 Style Theme

```
┌─────────────────────────────────────────┐
│           THEME COLORS                  │
├─────────────────────────────────────────┤
│                                         │
│  BUYER ROLE:                           │
│  ┌──────────────────────────────────┐  │
│  │ Primary Color: #007AFF (Blue)    │  │
│  │ Header Color: #007AFF            │  │
│  │ Buttons: #007AFF                 │  │
│  │ Accents: #007AFF                 │  │
│  └──────────────────────────────────┘  │
│                                         │
│  SELLER ROLE:                          │
│  ┌──────────────────────────────────┐  │
│  │ Primary Color: #FF9500 (Orange)  │  │
│  │ Header Color: #FF9500            │  │
│  │ Buttons: #FF9500                 │  │
│  │ Accents: #FF9500                 │  │
│  └──────────────────────────────────┘  │
│                                         │
│  COMMON:                                │
│  ┌──────────────────────────────────┐  │
│  │ Background: #f5f5f5 (Light Gray) │  │
│  │ Text Primary: #1a1a1a (Dark)     │  │
│  │ Text Secondary: #666 (Medium)    │  │
│  │ Borders: #ddd (Light)            │  │
│  └──────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

## 📊 State Management Strategy

```
┌──────────────────────────────────┐
│      Global Auth State           │
│      (AuthContext)               │
│                                  │
│  const [userRole, setUserRole]   │
│  const [isAuthenticated, ...]    │
│  const [user, setUser]           │
│                                  │
│  Functions:                      │
│  - selectRole(role)              │
│  - login(email, password)        │
│  - logout()                      │
│                                  │
└──────────────────────────────────┘
           │
    useAuth() hook
           │
  ┌────────┴─────────┬──────────┐
  │                  │          │
  ▼                  ▼          ▼
Screen 1         Screen 2    Screen 3
(Access         (Access     (Access
 state)          state)      state)
```

---

**This diagram structure helps visualize:**
- ✅ User flow through the app
- ✅ Component hierarchy
- ✅ Data flow between components
- ✅ Authentication flow
- ✅ File organization
- ✅ Color schemes for each role
